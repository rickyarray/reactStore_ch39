# git

# initialize a foldere as a git repo
git init

# setup the remove URL
git remote add origin <ULR>


### Push the first time
# crate a commit
git add .
git commit -m "ANY MESSAGE"
git push -u origin master


### Push future times
git add .
git commit -m "ANY Message"
git puh